<?php
    //isset --> TRUE si la variable esta inicializada y no es NULL
    // empty --> TRUE si la variable no esta inicializada o su valor es FALSE
    // is_int --> Para comprobar el tipo de dato de $var


    //strlen --> Devuelve la longitud de $cad
    //explode --> Parte una cadena utilizando $token como seprador, devuelve un array de cadenas
    //strtolower -->Devuelve la cadena en minusculas

    //sort --> Ordena el array por valor en order ascendente
    //array_values --> devuelve los valores de arr 
    //count --> devuelve el numero de elementos del array 
    $a = isset($_GET['variable']);
    $b = isset($_GET['cadena']);
    $c = isset($_GET['array1']);
    $d = isset($_GET['array2']);
    $e = isset($_GET['array3']);
    $f = isset($_GET['array4']);
    




?>
